name: 'supplylens'
version: '1.0.0'
config-version: 2

profile: 'supplylens'

model-paths: ["models"]
clean-targets: ["target", "dbt_packages"]

models:
  supplylens:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
